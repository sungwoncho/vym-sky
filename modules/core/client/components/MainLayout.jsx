import React from 'react';

const MainLayout = ({content}) => (
  <div>
    <header>
      header
    </header>
    <main>
      {content}
    </main>
    <footer>
      footer
    </footer>
  </div>
);

export default MainLayout;
